/*
* arrays in Kotlin is quite different in Python or JS
* in Python, we called arrays as list.
* the way we defined list(arrays) is, either list() or []
* same goes with JS
* */

fun main() {
    val myArray = arrayOf(3,2,6,4)
    println(myArray[-1])

    // myArray.map { mine -> println(mine) }
}